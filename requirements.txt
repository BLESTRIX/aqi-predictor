# ── Data Processing ──────────────────────────────────────────
pandas>=2.2.0,<3.0.0
numpy>=2.1.0,<2.3.0            # 2.1+ has cp313 wheels; no compilation needed

# ── Machine Learning ─────────────────────────────────────────
scikit-learn>=1.5.0,<1.7.0
xgboost>=2.1.0,<3.0.0

# ── HTTP & Config ────────────────────────────────────────────
requests>=2.31.0,<3.0.0
pyyaml>=6.0.1,<7.0.0
python-dotenv>=1.0.1,<2.0.0

# ── Validation ───────────────────────────────────────────────
pydantic>=2.8.0,<3.0.0
pydantic-settings>=2.4.0,<3.0.0

# ── UI & Visualization ───────────────────────────────────────
streamlit>=1.35.0,<2.0.0
plotly>=5.20.0,<6.0.0

# ── Explainability ───────────────────────────────────────────
numba>=0.61.0,<0.62.0          # 0.61 = first release with cp313 wheels + numpy 2.x support
shap>=0.46.0,<0.48.0           # 0.46+ targets numba 0.61+

# ── Feature Store ────────────────────────────────────────────
hopsworks==5.0.*        # 4.6+ is numpy 2.x compatible

# ── Testing ──────────────────────────────────────────────────
pytest>=8.0.0,<9.0.0